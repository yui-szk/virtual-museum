run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - gofmt
    - goimports
    - unused

issues:
  exclude-use-default: false

